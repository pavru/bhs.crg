# Следующие задачи (зачёркивай выполненные задачи)

1. ~~В наборы данных добавить файлы PDF, один файл - несколко наборов (один или несколько листов). Набор данных PDF должен поодерживать извлечение реквизитов листов (возможно с ИИ). Набор должен поодерживать скалярное и множественное связывание с документом. В связи с этой доработкой добавить функциональный тэг документа "Схема". Разбор штампов по ГОСТ 21.101-2020~~ — реализовано (загрузка PDF → распознавание штампов по ГОСТ Р 21.101-2020 → реестр листов → биндинг к документу и, отдельно, к записям каталога через обобщённый DataSetBinding с read-only полями и индикатором источника)
1. ~~Поля ключи полей и коды документов если не заполнены должны формироваться на основе наименования по схеме Camel. Если поля ключа/кода пустое или ранее сформировано автоматически, то при вводе пользователем наимнование надо формировать новое значение~~
1. При генерации документа нужна поддержка генерации нескольких документов, по разным шаблонам.
1. В шаблоне сделать доступ к данным родительского документа от дельным JSON, в typst примерно так: _#let p = json("parent.json")_
1. ~~Возмножно нужно ввести тэг для типа документа "Проектная документация"~~ — реализовано (`type.projectDocumentation`)
1. ~~Всё таки убрать кнопки сохранить, сохранить и закрыть, отмена в диалоге редактора документа со вкладок где изменения сохраняются сразу, оставить информационное сообзение "Изменения на этой вкладке применяются сразу"~~
1. ~~Набор данных xml, надо формировать наборы на основе задаваемых выражений xpath или xquery (только бесплатные библиотеки, обсудить), формирование наборов по элементам верхнего уровня исключить. Нужен builder выражений, чтобы им мог пользоваться минимально подготовленный человек~~ — реализовано на встроенном XPath 1.0 (без XQuery), builder с raw-fallback
1. ~~В главном меню исключить раздел каталог этот функционал покрывается общими данными~~
1. ~~Объяснить назначение вкладки "Связи" возможно нужно удалить~~ — удалена (был сырой JSON без UI-пикера, недоделанный legacy-путь; функцию можно переделать позже, если понадобится связывать документ с сущностью каталога)
1. ~~Интерфейс страница "типы полей" должны выглядеть аналогично страницам составных типов и типов документов~~
1. ~~На страницах типов ввести группировки, тип может быть перемещён в любую группу, группы должны иметь возможность свернуть/развернуть, по умолчанию свернуты, на странице может быть развернута только одна группа~~
1. ~~Ввести функциональные тэги для набора данных, в частности PDF "имеет титульный лист", "имеет обложку", "имеет основную надпись" по ГОСТ Р 21.101—2020. Распознанные данные должны кэшироваться до обновления набора данных~~ — реализовано (`dataset.hasCover`/`hasTitlePage`/`hasTitleBlock`, кэш на `DataSetSource.CachedData`)
1. Нужно добавить функцию генерации всего комплекта с возможностью задания порядка документов в файле.
1. В шаблоных нужен досутп к вложенным файлам докуметов, для вставки в сгенерированный документ
1. Нужно реализовать поиск документов по комплектам
1. ~~Везде где есть пары "Наименование"-"Ключ/Код" в интерфейсе сначало должно идти Наименование~~