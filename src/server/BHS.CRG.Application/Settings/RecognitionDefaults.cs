namespace BHS.CRG.Application.Settings;

/// <summary>
/// Значения по умолчанию для движков распознавания — на случай, когда ни база, ни конфигурация
/// ничего не задали.
///
/// Одной константой, а не строкой в каждом месте, где она понадобилась: до issue #799 «gemini-2.5-flash»
/// было записано в четырёх файлах, и обновить его значило найти все четыре. Ровно так протухают
/// умолчания — не потому, что о них забыли, а потому, что их некуда было записать один раз.
/// </summary>
public static class RecognitionDefaults
{
    /// <summary>
    /// Модель Gemini по умолчанию. Выбрана замером (issue #799, 2026-08-20): на документе в 28 страниц
    /// при полном наборе полей она единственная из проверенных уложилась в срок ответа — 64 с против
    /// таймаута у «gemini-2.5-flash» и «gemini-3.5-flash».
    /// </summary>
    public const string GeminiModel = "gemini-3.5-flash-lite";
}
