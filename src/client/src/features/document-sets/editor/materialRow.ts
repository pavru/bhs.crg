/**
 * Строка материала на вкладке документов качества: `key` — составной ключ идентичности (им связка и
 * заводится, и ищется), `label` — то же для человека, `idValues` — непустые значения для оценки
 * релевантности.
 *
 * Своим модулем, а не внутри вкладки: тип нужен и таблице, и модалке подбора, и самой вкладке.
 * Объяви его в любом из этих файлов — два других импортировали бы тип из модуля с компонентами, а
 * вкладка и модалка сослались бы друг на друга по кругу.
 */
export interface MaterialRow { key: string; label: string; idValues: string[] }
