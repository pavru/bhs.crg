# Нижняя граница Node из engines — не пожелание: при более старом Node установка должна
# падать с внятным EBADENGINE, а не собираться и ломаться потом невнятно. Порог задаёт
# react-router 8 (>= 22.22.0), см. package.json и deploy/Dockerfile.web.
engine-strict=true
