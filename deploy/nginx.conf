server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Загрузка сканов, наборов данных, изображений
    # Согласовано с UploadLimits.MaxAnywhere (issue #482): наибольший наш предел —
    # архив восстановления бэкапа (500 МБ) плюс запас на обвязку multipart.
    # Порог ниже сделает наш предел недостижимым, а отказ придёт HTML-страницей nginx,
    # в которой нет поля error — клиент покажет голое сообщение axios.
    client_max_body_size 508m;

    # REST API → бэкенд
    location /api/ {
        proxy_pass http://api:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }

    # SignalR (WebSocket) → бэкенд
    location /hubs/ {
        proxy_pass http://api:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
    }

    # SPA — все прочие маршруты отдаём index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
