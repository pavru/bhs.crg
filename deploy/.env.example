# ── Скопируйте в .env и заполните значения ──────────────────────────────────────
# cp deploy/.env.example deploy/.env

# Версия поставки — тег образов api и web в GHCR (список версий: страница Releases репозитория).
# Обновление системы = смена этого значения → pull → up -d, см. docs/DEPLOYMENT.md §8.
APP_VERSION=0.135.0

# PostgreSQL
POSTGRES_DB=bhs_crg
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_strong_password

# MinIO (объектное хранилище). Это учётные данные АДМИНИСТРАТОРА хранилища: их знание даёт
# полный доступ ко всем файлам системы. Логин по умолчанию не оставляйте — он общеизвестен.
MINIO_ROOT_USER=change_me_storage_admin
MINIO_ROOT_PASSWORD=change_me_minio_password
MINIO_BUCKET=bhs-crg
# Адрес и порт консоли MinIO. Compose публикует консоль ТОЛЬКО на петлю (127.0.0.1) — заходить
# на неё с рабочей машины через SSH-туннель. Открывать наружу консоль под учёткой администратора
# хранилища не нужно: это второй вход ко всем данным системы, в обход приложения.
MINIO_CONSOLE_BIND=127.0.0.1
MINIO_CONSOLE_PORT=9001

# JWT — длинный случайный секрет (минимум 32 символа!)
# Сгенерировать:  openssl rand -base64 48
# Значение ниже — заглушка: приложение с ней НЕ ЗАПУСТИТСЯ, это проверяется при старте.
JWT_KEY=CHANGE_ME_TO_A_LONG_RANDOM_SECRET_AT_LEAST_32_CHARS

# Веб-интерфейс (порт на хосте)
WEB_PORT=8080

# Сети, чьему заголовку X-Forwarded-For сервер доверяет (CIDR через запятую). Из этого заголовка
# берётся адрес клиента для пределов частоты обращений. Пусто — петля и частные диапазоны, чего
# достаточно, пока API доступен только из сети Compose. Сузьте до сети своего обратного прокси,
# если API виден откуда-то ещё. Негодная запись останавливает запуск.
FORWARDED_TRUSTED_NETWORKS=

# Публичный адрес системы (напр. https://docs.example.ru) — внешний URL, по которому
# пользователи открывают систему. Подставляется ссылкой в письма: сброс пароля,
# подтверждение/смена email, отправка крупного комплекта (> 15 МБ).
# ВАЖНО: без него письма со ссылками НЕ отправляются (действие вернёт понятную ошибку).
# Обязателен, если пользуетесь самостоятельным сбросом пароля / подтверждением почты.
APP_PUBLIC_URL=

# Ollama — локальная модель распознавания сканов (опционально, тяжёлая ~6 ГБ)
OLLAMA_MODEL=qwen2.5vl:7b

# Предел размера резервной копии, МБ. Восстановление отвергает архив крупнее — и до issue #711
# экспорт про этот предел не знал, то есть система делала копии, которые сама же откажется
# принять. Вес копии задаёт библиотека документов качества: ~1,3 МБ на сертификат, и растёт она
# годами. Текущий вес против предела виден в интерфейсе: Настройки → Резервное копирование.
# Этой же переменной задаётся client_max_body_size у веб-сервера — менять что-то ещё не нужно.
# Допустимо от 16 до 10240; негодное значение останавливает запуск.
BACKUP_MAX_ARCHIVE_MB=500

# Потолки памяти контейнеров. Тяжёлые операции запускает пользователь, и без потолка неудачный
# шаблон или большой архив уводят в своп весь хост. Слишком низкое значение выглядит как
# перезапуск контейнера без внятной причины — см. docs/DEPLOYMENT.md §10.
API_MEMORY_LIMIT=3g
OLLAMA_MEMORY_LIMIT=10g

# Внешние сервисы (опционально; нужны только для документов качества)
# Anthropic / Gemini — распознавание реквизитов; Serper — веб-поиск документов
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
SERPER_API_KEY=
